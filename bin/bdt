#!/usr/bin/env node

require('commander')
  .version(require('../package').version)
  .usage('<command> [options]')
  .command('deploy', 'Deploy to Blued App Engine')
  .command('init', 'Init Blued App Engine Template')
  .parse(process.argv)
